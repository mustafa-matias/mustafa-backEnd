<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<link href="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
<script src="//maxcdn.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
<link rel="stylesheet" href="../css/product.css">
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
    integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <div class="container mt-5">
        <div class="card">
            <div class="container-fliud">
                <div class="wrapper row">
                    <div class="preview col-md-6">
                        <div class="preview-pic tab-content">
                            <div class="tab-pane active" id="pic-1">
                                <img src="{{product.thumbnail}}" class="img-small" alt="Product Image" />
                            </div>
                        </div>
                    </div>
                    <div class="details col-md-6">
                        <div>
                            <div class="d-flex justify-content-between">
                                <span class="product-title h2">{{product.title}}</span>
                                <span class="fst-italic text-capitalize text-secondary">{{product.category}}</span>
                            </div>
                            <p class="product-description">{{product.description}}</p>
                            <h4 class="price">Precio: <span class="h3">${{product.price}}</span></h4>
                            <p class="vote"><strong>Stock:</strong> {{product.stock}} unidades</p>
                            <div class="product-info">
                                <div class="d-flex align-items-center">
                                    <span class="mr-2">Rating:</span>
                                    <div class="rating">
                                        <span class="fa fa-star checked"></span>
                                        <span class="fa fa-star checked"></span>
                                        <span class="fa fa-star checked"></span>
                                        <span class="fa fa-star checked"></span>
                                        <span class="fa fa-star"></span>
                                    </div>
                                </div>
                                <p class="text-muted small mt-2">Comentarios (0)</p>
                            </div>
                        </div>
                        {{#if usuario}}
                        <div class="row mt-3">
                            <div class="col-md-6">
                                <form action="http://localhost:8080/api/carts/{{usuario.cart}}/product/{{product._id}}" method="POST">
                                    <button class="btn btn-outline-success" id="addToCartProduct">Agregar al Carrito</button>
                                </form>
                            </div>
                            <div class="col-md-6">
                                <a href="/products" class="btn btn-outline-secondary">Volver a la Tienda</a>
                            </div>
                        </div>
                        {{else}}
                        <div class="row mt-3">
                            <div class="col-md-12">
                                <a href="/sessions/login" class="btn btn-danger btn-lg">Inicie sesión para comprar</a>
                                <a href="/products" class="btn btn-secondary btn-lg">Volver a la tienda</a>
                            </div>
                        </div>
                        {{/if}}
                    </div>
                </div>
            </div>
        </div>
    </div>
<script src="/js/product.js"></script>